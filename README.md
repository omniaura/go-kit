# go-kit
Standard Library Extensions

Go modules in this repository target Go 1.25.5+ and prefer the Go 1.26.1
toolchain when available.

To test all workspace modules locally, run `./scripts/test-all.sh`.
